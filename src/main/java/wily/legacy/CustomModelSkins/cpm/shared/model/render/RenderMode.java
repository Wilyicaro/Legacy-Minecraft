package wily.legacy.CustomModelSkins.cpm.shared.model.render;

public enum RenderMode {
    DEFAULT(0), NORMAL(0);
    private final int renderLayer;

    RenderMode(int renderLayer) {
        this.renderLayer = renderLayer;
    }
}
